type SectionTitleProps = {
    eyebrow?: string;
    title: string;
    subtitle: string;
};
export default function SectionTitle({ eyebrow, title, subtitle }: SectionTitleProps): import("react").JSX.Element;
export {};
